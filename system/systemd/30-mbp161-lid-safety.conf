# Optional user preference, not required or installed by the Falcon patch.
# Let the compositor handle lid closure instead of logind initiating suspend.
# Review the compositor's lid policy before using this. Explicit suspend is
# still possible, but normal suspend/resume with Falcon remains unvalidated.
[Login]
HandleLidSwitch=ignore
